name: Deploy MkDocs to GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Build all wikis
        run: |
          # Build each wiki
          cd everywhere && mkdocs build && cd ..
          cd multi-mode && mkdocs build && cd ..
          cd systemx && mkdocs build && cd ..
          cd modulex && mkdocs build && cd ..
          
          # Create main site directory
          mkdir -p site
          
          # Copy built wikis to site
          cp -r everywhere/site site/everywhere
          cp -r multi-mode/site site/multi-mode
          cp -r systemx/site site/systemx
          cp -r modulex/site site/modulex
          
          # Copy main index
          cp index.html site/index.html

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./site

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
